#### Ticket:

<!--
Filled in from the PR title. Name the PR `KEY-123 | Title` and the link
between the ticket markers is written for you. It is rewritten each time
the title changes, so edit the title, not the link.
-->

<!-- ticket:start -->
- {jira-link}
<!-- ticket:end -->

#### Changes:

<!-- One bullet per change this branch makes. -->

-

#### QA Notes:

<!--
The preview link is filled in from the repo's STORE_DOMAIN variable.
Replace {theme-id} with the numeric theme id. `npm run themes` lists the
ids. Then add the steps to QA below the link.

The preview_theme_id parameter is consumed on the first request, so the
URL looks the same on a preview and on the live theme. To confirm which
theme a page is on, read window.Shopify.theme in the console and check
`role` and `id`. role is "unpublished" on a preview and "main" on live.
Do not go by the URL.
-->

- {preview-link}

#### Screenshots: (Optional)

<!-- Post any important screenshots -->
